import { blogPost } from './blogPost';
import { useCase } from './useCase';
import { blockContent } from './blockContent';
import { contentImage } from './contentImage';

export const schemaTypes = [blogPost, useCase, blockContent, contentImage];
